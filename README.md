# Web-App-for-Databse-class

This is just a small class project, that holds both the database and the web stuff for a class manager system.
The system just checks for available classes and those with higher clearence have more control than the students.
